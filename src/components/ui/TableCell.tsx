@@ .. @@
 const TableCell: React.FC<TableCellProps> = ({ children, className = '', header = false }) => {
 }
-  const baseClasses = 'px-6 py-4 text-sm';
+  const baseClasses = 'px-3 sm:px-6 py-4 text-sm';
   const headerClasses = 'font-medium text-gray-900 uppercase tracking-wider';
   const cellClasses = 'text-gray-900';